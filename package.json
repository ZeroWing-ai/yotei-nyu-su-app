{
  "name": "schedule-news-dashboard",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "concurrently -n server,client \"npm --prefix server start\" \"npm --prefix client run preview\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}

